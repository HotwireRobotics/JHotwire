# Jarvis Compatibility Layer

Jarvis has been renamed to **HELIX**.

Use these paths instead:

- `helix/README.md`
- `helix/config.json`
- `python -m helix.main`
- `python run_helix.py`

The `jarvis/` package is kept as a compatibility bridge so existing scripts continue to run while migrating.
